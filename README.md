# Web Scraper

![Web Scraper preview](preview.png)

Simple title scraper with `httpx` and `BeautifulSoup`.

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
scrape titles https://example.com https://python.org
```
